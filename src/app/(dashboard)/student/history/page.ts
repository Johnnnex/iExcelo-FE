import type { Metadata } from "next";
import History from "./History";

export const metadata: Metadata = {
  title: "iExcelo - Student | Exam History",
  robots: { index: false, follow: false },
};

export default History;
