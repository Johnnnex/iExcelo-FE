import type { Metadata } from "next";
import ExamInfo from "./ExamInfo";

export const metadata: Metadata = {
  title: "iExcelo - Student | Exam Info",
  robots: { index: false, follow: false },
};

export default ExamInfo;
