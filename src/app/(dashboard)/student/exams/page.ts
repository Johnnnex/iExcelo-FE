import type { Metadata } from "next";
import Exams from "./Exams";

export const metadata: Metadata = {
  title: "iExcelo - Student | Exams",
  robots: { index: false, follow: false },
};

export default Exams;
