import type { Metadata } from "next";
import Mock from "./Mock";

export const metadata: Metadata = {
  title: "iExcelo - Student | Mock Exam",
  robots: { index: false, follow: false },
};

export default Mock;
