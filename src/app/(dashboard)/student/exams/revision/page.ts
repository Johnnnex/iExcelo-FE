import type { Metadata } from "next";
import Revision from "./Revision";

export const metadata: Metadata = {
  title: "iExcelo - Student | Revision",
  robots: { index: false, follow: false },
};

export default Revision;
