import type { Metadata } from "next";
import Timed from "./Timed";

export const metadata: Metadata = {
  title: "iExcelo - Student | Timed Exam",
  robots: { index: false, follow: false },
};

export default Timed;
