import type { Metadata } from "next";
import Password from "./Password";

export const metadata: Metadata = {
  title: "iExcelo - Student | Password & Security",
  robots: { index: false, follow: false },
};

export default Password;
