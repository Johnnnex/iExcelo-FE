import type { Metadata } from "next";
import Billing from "./Billing";

export const metadata: Metadata = {
  title: "iExcelo - Student | Billing",
  robots: { index: false, follow: false },
};

export default Billing;
