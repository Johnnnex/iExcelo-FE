import type { Metadata } from "next";
import Messages from "./Messages";

export const metadata: Metadata = {
  title: "iExcelo - Student | Messages",
  robots: { index: false, follow: false },
};

export default Messages;
