import type { Metadata } from "next";
import Commissions from "./Commissions";

export const metadata: Metadata = {
  title: "iExcelo - Affiliates | Commissions",
  robots: { index: false, follow: false },
};

export default Commissions;
