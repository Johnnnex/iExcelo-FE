import type { Metadata } from "next";
import Students from "./Students";

export const metadata: Metadata = {
  title: "iExcelo - Affiliates | Students",
  robots: { index: false, follow: false },
};

export default Students;
