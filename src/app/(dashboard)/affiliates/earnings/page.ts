import type { Metadata } from "next";
import Earnings from "./Earnings";

export const metadata: Metadata = {
  title: "iExcelo - Affiliates | Earnings",
  robots: { index: false, follow: false },
};

export default Earnings;
