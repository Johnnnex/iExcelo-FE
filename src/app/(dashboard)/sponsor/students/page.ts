import type { Metadata } from "next";
import Students from "./Students";

export const metadata: Metadata = {
  title: "iExcelo - Sponsor | Students",
  robots: { index: false, follow: false },
};

export default Students;
