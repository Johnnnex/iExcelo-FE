import { Metadata } from "next";
import GivebackHistory from "./GivebackHistory";

export const metadata: Metadata = {
  title: "iExcelo - Sponsor | Giveback History",
};

export default GivebackHistory;
