import type { Metadata } from "next";
import Messages from "./Messages";

export const metadata: Metadata = {
  title: "iExcelo - Sponsor | Messages",
  robots: { index: false, follow: false },
};

export default Messages;
