import type { Metadata } from "next";
import Profile from "./Profile";

export const metadata: Metadata = {
  title: "iExcelo - Sponsor | Profile",
  robots: { index: false, follow: false },
};

export default Profile;
