import type { Metadata } from "next";
import AffiliateRedirect from "./AffiliateRedirect";

export const metadata: Metadata = {
  title: "iExcelo - Redirecting...",
  robots: { index: false, follow: false },
};

export default AffiliateRedirect;
